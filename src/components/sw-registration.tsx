'use client';

/**
 * Service worker registration is now handled automatically by next-pwa.
 * This component is kept as an empty shell to maintain compatibility with layout imports.
 */
export function SwRegistration() {
  return null;
}